---
type Props = {
	class?: string;
};

const { class: className, ...attrs } = Astro.props as Props;
---

<p
	class:list={[
		"my-5 text-[20px] leading-relaxed text-pretty text-zinc-800 [&:has(img)]:my-8 [&:has(img)]:not-prose",
		className,
	]}
	{...attrs}
>
	<slot />
</p>
