---
import Main from "./MainLayout.astro"

const { title, description, index } = Astro.props
---

<Main title={title} description={description} index={index}>
	<main
		class="prose prose-img:rounded-lg max-w-3xl mx-auto prose-blue xl:prose-xl prose-lg"
	>
		<slot />
	</main>
</Main>
